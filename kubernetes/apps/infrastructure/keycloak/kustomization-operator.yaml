apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: keycloak-operator
  namespace: flux-system
spec:
  interval: 1h
  path: ./kubernetes/apps/infrastructure/keycloak/operator
  prune: true
  wait: true
  dependsOn:
    - name: keycloak-crds
  sourceRef:
    kind: GitRepository
    name: flux-system
