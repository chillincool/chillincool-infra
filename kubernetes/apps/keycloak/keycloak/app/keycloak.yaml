---
apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak
  namespace: keycloak
spec:
  instances: 1
  db:
    vendor: postgres
    host: keycloak-pg-rw
    usernameSecret:
      name: keycloak-db-secret
      key: username
    passwordSecret:
      name: keycloak-db-secret
      key: password
  http:
    httpEnabled: true
  hostname:
    hostname: https://login.chillincool.net
    backchannelDynamic: true
  proxy:
    headers: xforwarded
  ingress:
    enabled: false
  additionalOptions:
    - name: truststore-paths
      value: /opt/keycloak/conf/truststores/ldap
  unsupported:
    podTemplate:
      metadata:
        annotations:
          secret.reloader.stakater.com/reload: keycloak-ldap-client-tls
      spec:
        containers:
          - name: keycloak
            volumeMounts:
              - name: ldap-client-tls
                mountPath: /opt/keycloak/conf/truststores/ldap
                readOnly: true
        volumes:
          - name: ldap-client-tls
            secret:
              secretName: keycloak-ldap-client-tls
              defaultMode: 0444
