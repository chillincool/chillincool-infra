---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: reloader
  namespace: default
spec:
  interval: 30m
  chart:
    spec:
      chart: reloader
      version: "1.2.1"
      sourceRef:
        kind: HelmRepository
        name: stakater
        namespace: flux-system
  install:
    remediation:
      retries: 3
    createNamespace: false
  upgrade:
    cleanupOnFail: true
  values:
    reloader:
      watchGlobally: false
      namespaceSelector: ""
      deployment:
        replicas: 1
