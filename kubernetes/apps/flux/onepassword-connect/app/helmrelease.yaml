---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: onepassword-connect
  namespace: onepassword
spec:
  interval: 30m
  chart:
    spec:
      chart: connect
      version: "1.15.1"
      sourceRef:
        kind: HelmRepository
        name: 1password
        namespace: flux-system
  install:
    crds: Create
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
  values:
    connect:
      credentialsName: "onepassword-credentials"  # Restored to use the existing secret
      credentialsKey: "MXBhc3N3b3JkLWNyZWRlbnRpYWxzLmpzb24="  # Base64-encoded string of '1password-credentials.json'

    operator:
      create: true
      autoRestart: true
      watchNamespace: []
      tokenSecret: onepassword-token
      tokenSecretKey: token
