---
# AuthorizationPolicy to require OAuth2-proxy authentication for Overseerr
# In ambient mode, this applies at the waypoint proxy level
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: overseerr-oauth2-authz
  namespace: media
spec:
  selector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: media-waypoint
  action: CUSTOM
  provider:
    name: oauth2-proxy
  rules:
    - to:
        - operation:
            hosts: ["overseerr.chillincool.net"]
