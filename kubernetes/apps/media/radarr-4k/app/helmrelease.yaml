---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: radarr-4k
  namespace: media
spec:
  interval: 30m
  chart:
    spec:
      chart: radarr
      version: 0.x.x
      sourceRef:
        kind: HelmRepository
        name: chillincool
        namespace: flux-system
  install:
    remediation:
      retries: 3
    createNamespace: false
    crds: Create
  upgrade:
    cleanupOnFail: true
    crds: CreateReplace
  values:
    nameOverride: radarr-4k

    httpRoute:
      enabled: true
      parentRefs:
        - name: media-gateway-istio
          namespace: media
          sectionName: https-radarr-4k
      hostnames:
        - radarr-4k.chillincool.net
      rules:
        - matches:
            - path:
                type: PathPrefix
                value: /
          backendRefs:
            - name: radarr-4k
              port: 7878

    initContainers:
      - name: create-directories
        image: alpine:latest
        command:
          - sh
          - -c
          - |
            # Only create directories if they don't exist (idempotent)
            mkdir -p /storage/symlinks/downloads/movies-4k
            mkdir -p /storage/symlinks/movies-4k
            mkdir -p /config

            # Only chown if not already correct (avoids touching existing files)
            if [ "$(stat -c '%u:%g' /storage/symlinks 2>/dev/null || stat -f '%u:%g' /storage/symlinks)" != "568:568" ]; then
              echo "Setting ownership to 568:568"
              chown -R 568:568 /storage/symlinks
            else
              echo "Ownership already correct, skipping chown"
            fi

            # Ensure config directory has correct ownership
            if [ "$(stat -c '%u:%g' /config 2>/dev/null || stat -f '%u:%g' /config)" != "568:568" ]; then
              echo "Setting config ownership to 568:568"
              chown -R 568:568 /config
            else
              echo "Config ownership already correct, skipping chown"
            fi
        volumeMounts:
          - name: symlinks
            mountPath: /storage/symlinks
          - name: config
            mountPath: /config

    envFrom:
      - secretRef:
          name: radarr-4k-secret

    service:
      type: ClusterIP
      port: 7878
      targetPort: http

    podAnnotations:
      configmap.reloader.stakater.com/reload: decypharr-values
      secret.reloader.stakater.com/reload: rclone-secret-realdebrid

    podSecurityContext:
      fsGroup: 568
      fsGroupChangePolicy: OnRootMismatch

    securityContext:
      runAsNonRoot: true
      runAsUser: 568
      runAsGroup: 568
      readOnlyRootFilesystem: false

    resources:
      requests:
        cpu: 50m
        memory: 256Mi
      limits:
        cpu: 2
        memory: 2Gi

    persistence:
      config:
        enabled: true
        type: persistentVolumeClaim
        existingClaim: radarr-4k-config
        globalMounts:
          - path: /config
      cache:
        enabled: true
        type: persistentVolumeClaim
        existingClaim: radarr-4k-cache
        globalMounts:
          - path: /config/MediaCover
      backups:
        enabled: true
        type: persistentVolumeClaim
        existingClaim: backups
        globalMounts:
          - path: /storage/backups
            subPath: radarr-4k
      realdebrid:
        enabled: true
        type: persistentVolumeClaim
        existingClaim: realdebrid-webdav
        globalMounts:
          - path: /storage/realdebrid
      symlinks:
        enabled: true
        type: persistentVolumeClaim
        existingClaim: symlinks
        globalMounts:
          - path: /storage/symlinks
      dsm1:
        enabled: true
        type: persistentVolumeClaim
        existingClaim: dsm1-data
        globalMounts:
          - path: /storage/dsm1
      tmp:
        enabled: true
        type: emptyDir
        globalMounts:
          - path: /tmp
