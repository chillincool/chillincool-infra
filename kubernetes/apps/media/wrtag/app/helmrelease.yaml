---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wrtag
spec:
  chartRef:
    kind: OCIRepository
    name: wrtag
  interval: 1h
  values:
    controllers:
      wrtag:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/sentriz/wrtag
              tag: v0.34.0@sha256:69df2e15e13cb314a78c4c6b5c2246eab6f902c7d0515310c608aa3ba0820b72
            command: ["/usr/local/bin/wrtagweb"]
            env:
              WRTAG_ADDON: "lyrics musixmatch lrclib genius,replaygain"
              WRTAG_PATH_FORMAT: '/media/media/music/{{`{{ artists .Release.Artists | sort | join "; " | safepath }}`}}/({{`{{ .Release.ReleaseGroup.FirstReleaseDate.Year }}`}}) {{`{{ .Release.Title | safepath }}`}}{{`{{ if not (eq .ReleaseDisambiguation "") }}`}} ({{`{{ .ReleaseDisambiguation | safepath }}`}}){{`{{ end }}`}}/{{`{{ pad0 2 .Track.Position }}`}} {{`{{ .Track.Title | safepath }}`}}{{`{{ .Ext }}`}}'
              WRTAG_WEB_AUTH: disabled
              WRTAG_WEB_LISTEN_ADDR: :80
              WRTAG_WEB_DB_PATH: /data/wrtag.db
              WRTAG_WEB_PUBLIC_URL: https://wrtag.chillincool.net
              WRTAG_LOG_LEVEL: debug
              TZ: America/Chicago
            envFrom:
              - secretRef:
                  name: wrtag-secret
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  tcpSocket:
                    port: &port 80
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
            resources:
              requests:
                cpu: 50m
                memory: 128Mi
              limits:
                memory: 256Mi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        ports:
          http:
            port: *port
    route:
      wrtag:
        hostnames:
          - "{{ .Release.Name }}.chillincool.net"
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - identifier: app
                port: *port
    persistence:
      data:
        existingClaim: "{{ .Release.Name }}"
        globalMounts:
          - path: /data
      media:
        type: nfs
        server: dsm1.chillincool.net
        path: /volume2/data
        globalMounts:
          - path: /media
      tmp:
        type: emptyDir
